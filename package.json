{
  "name": "swagger-servers-url-merger",
  "version": "0.0.1",
  "description": "Merge each servers.url and path for multiple Swagger definitions.",
  "bin": {
    "swagger-servers-url-merger": "bin/swagger-servers-url-merger"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "swagger",
    "merge",
    "api",
    "versioning",
    "openapi"
  ],
  "author": "yuya sato",
  "license": "MIT",
  "dependencies": {
    "js-yaml": "^4.1.0",
    "yargs": "^17.6.2"
  },
  "files": ["bin/"]
}
